import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    position: "relative",
    left: 0,
    bottom: 120,
  },
  logo: {
    width: 180,
    height: 60,
    ///backgroundColor: '#F1EFEF',
    borderRadius: 2
  },
});
